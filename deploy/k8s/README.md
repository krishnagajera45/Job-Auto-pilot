# Kubernetes Deployment Notes

- Create namespaces per environment (dev/staging/prod).
- Use Helm or Kustomize for service deployments.
- Configure external secrets for API keys and database credentials.
- Enable HPA on API gateway, agent orchestration, and automation services.
